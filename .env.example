# ===========================================
# Environment Variables Configuration
# ===========================================
# Copy this file to .env.docker for local development
# or .env for production overrides
# ===========================================

# ==============================================================================
# Environment Settings
# ==============================================================================
ENVIRONMENT=development
# Options: development, staging, production
# - development: Enables debug mode, hot reload
# - staging: Production-like environment for testing
# - production: Optimized for performance, security enabled

# ==============================================================================
# MongoDB Configuration
# ==============================================================================
MONGO_INITDB_ROOT_USERNAME=admin
# MongoDB root username (default: admin)

MONGO_INITDB_ROOT_PASSWORD=password123
# MongoDB root password (IMPORTANT: Change in production!)
# For production, use a strong password (min 12 chars, mixed case, numbers, symbols)

MONGO_INITDB_DATABASE=appdb
# Initial database name to create

DATABASE_URL=mongodb://admin:password123@mongodb:27017/appdb?authSource=admin
# Full MongoDB connection string
# Format: mongodb://[username]:[password]@[host]:[port]/[database]?authSource=[auth_database]
# In production, consider using:
# DATABASE_URL=mongodb://admin:securepassword@mongodb:27017/appdb?authSource=admin&replicaSet=rs0

# ==============================================================================
# Redis Configuration
# ==============================================================================
REDIS_URL=redis://redis:6379
# Redis connection string
# Format: redis://[password]@[host]:[port]/[db_number]
# Example with password: redis://:password@redis:6379/0

REDIS_PASSWORD=redis-password
# Redis password (IMPORTANT: Change in production!)
# For production, use a strong password

# ==============================================================================
# Security Secrets - VERY IMPORTANT: CHANGE IN PRODUCTION!
# ==============================================================================
JWT_SECRET=your-jwt-secret-change-in-production-min-32-chars
# Secret key for JWT token signing
# Generate with: openssl rand -base64 32
# Must be at least 32 characters

OTP_SECRET=your-otp-secret-change-in-production-min-32-chars
# Secret key for OTP (One-Time Password) generation
# Generate with: openssl rand -base64 32
# Must be at least 32 characters

API_KEY=your-api-key-change-in-production
# API key for external service authentication
# Generate with: openssl rand -hex 32

# ==============================================================================
# Logging Configuration
# ==============================================================================
LOG_LEVEL=info
# Logging level: debug, info, warning, error, critical
# Use 'debug' for development, 'info' or 'warning' for production

# ==============================================================================
# Application Settings
# ==============================================================================
DEBUG=false
# Enable debug mode (development only)
# WARNING: Never set to true in production!

# ==============================================================================
# Frontend Environment Variables (passed via Dockerfile ARG)
# ==============================================================================
# NODE_ENV=development  # Set in docker-compose.yml
# VITE_API_URL=http://localhost:8000  # Can be overridden

# ==============================================================================
# Example Production Values (uncomment and modify)
# ==============================================================================
# ENVIRONMENT=production
# MONGO_INITDB_ROOT_PASSWORD=<strong-password-min-12-chars>
# REDIS_PASSWORD=<strong-password>
# JWT_SECRET=<generated-secure-random-string>
# OTP_SECRET=<generated-secure-random-string>
# API_KEY=<generated-secure-random-string>
# LOG_LEVEL=warning
# DEBUG=false

# ==============================================================================
# Docker Build Args (for production builds)
# ==============================================================================
# VERSION=1.0.0  # Image tag version
# BUILD_ENV=production
